// TodoList – renders a list of TodoItems

import React from 'react';
import App from '../../App'
import ReactDOM from 'react-dom';
import {TodoItem} from './todoitem'

const root = ReactDOM.createRoot(document.getElementById('root')!)

export const TodoList = () => {
    <TodoItem />
}